Y si en lugar de definir varias cláusulas de `cuantoSabePersonaje/2`, ¿hubieramos definido varios predicados....

```prolog
cuantoSabePersonajeLannister(lannister(_), mucho).
cuantoSabePersonajeStark(stark(_, _), poco).
% etc
```

...sería lo mismo?
